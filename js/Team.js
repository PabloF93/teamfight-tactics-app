class Team {
			constructor(name, champions) {
				this.name = name;
				this.champions = champions;
			}

			addDescription(description) {
				this.description = description;
			}

			addTier(tier) {
				this.tier = tier;
			}

		}